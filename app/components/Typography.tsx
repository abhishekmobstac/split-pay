
// define a type for typography props
type TypographyProps = {
    variant: string;
    className?: string;
    children: string;
};

const Typography = ({ variant, className, children }: TypographyProps) => {
    return (
        <div>
            
        </div>
    )
}